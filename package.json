{
  "name": "tree-token-challenge",
  "version": "1.0.0",
  "description": "ERC-20 TreeToken challenge with frontend integration",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.4",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.5.0",
    "ethers": "^5.8.0",
    "express": "^5.1.0"
  }
}
